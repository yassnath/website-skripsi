import LoginLayer from "@/components/LoginLayer";

export const metadata = {
  title: "Login | CV ANT",
  description: "Login to Website",
};

const Page = () => {
  return <LoginLayer />;
};

export default Page;
